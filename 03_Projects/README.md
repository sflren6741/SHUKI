# 03_Projects — プロジェクト

期限つきで、複数のタスク・ログ・ナレッジが束になって初めて意味を持つ取り組み（`type: project`）。各ハブは `プロジェクト/<名前>.md`。子ノートは Bases で `parent` 集約。完了したら `status: done` → 後で `status: archived`（物理移動しない）。

> 骨組み例：`プロジェクト/_example-project.md`。
