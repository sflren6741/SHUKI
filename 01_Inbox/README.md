# 01_Inbox — 未処理キャプチャ

何でもここに放り込む（メモ・URL・思いつき）。`orchestrator` が定期的に振り分け、frontmatter を補完して正式な置き場所（タスク／ログ／ナレッジ／プロジェクト）へ移す。`status: done` にするのは情報を正式ファイルに**移行し終えてから**。判断軸は「消すか」ではなく「内容が転記済みか」。

> URL キャプチャ：`type: 🔗URL` と `url:` を設定すると knowledge エージェントがリンク付きノートに変換する。骨組み例：`インボックス/_example-inbox.md`。
