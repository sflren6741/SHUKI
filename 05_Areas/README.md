# 05_Areas — 継続責任領域

継続的な責任領域。各 Area は **単一ハブノート** `<area>.md` のみで表す（サブフォルダを作らない・例外なし）。ハブは散文＋Bases で `area` 一致ノートを集約。派生コンテンツはタスク／ログ／リソースに振り分ける。各ノートの `area:`（プレーンテキスト）でハブに自動で紐づく。

> 自分の Area を定義して使う（仕事・健康・お金・人間関係 … など）。骨組み例：`_example-area.md`。
